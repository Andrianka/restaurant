- if user_signed_in?
  .pull-right= link_to 'Logout', destroy_user_session_path, :method => :delete, class:"btn"
  .pull-right= link_to 'Profile', '#', class:"btn"
- else
  .pull-right= link_to 'Login', new_user_session_path, class:"btn"
  .pull-right= link_to 'Sign up', new_user_registration_path, class:"btn"
